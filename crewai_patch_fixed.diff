diff --git a/app.js b/app.js
--- a/app.js
+++ b/app.js
@@ -4,7 +4,7 @@ let timerStartTime = null;
 let timerElapsed = 0;
 let currentProject = null;
 let timeEntries = [];
-let projects = ['Projekt A', 'Projekt B', 'Projekt C'];
+let projects = [];
 
 // DOM elements
 const projectSelect = document.getElementById('project-select');
@@ -48,6 +48,14 @@ function handleProjectSelect() {
     updateCurrentProjectDisplay();
 }
 
+function generateUniqueColor() {
+    const letters = '0123456789ABCDEF';
+    let color = '#';
+    for (let i = 0; i < 6; i++) {
+        color += letters[Math.floor(Math.random() * 16)];
+    }
+    return color;
+}
+
 function handleAddProject() {
     const newProject = customProjectInput.value.trim();
-    if (newProject && !projects.includes(newProject)) {
-        projects.push(newProject);
+    if (!newProject) {
+        return;
+    }
+    
+    // Check if project already exists (by name)
+    const existingProject = projects.find(p => (typeof p === 'string' ? p : p.name) === newProject);
+    if (existingProject) {
+        alert('Project name already exists.');
+        return;
+    }
+    
+    // Validate project name length
+    if (newProject.length < 1 || newProject.length > 50) {
+        alert('Project name must be between 1 and 50 characters.');
+        return;
+    }
+    
+    const projectColor = generateUniqueColor();
+    const projectData = { name: newProject, color: projectColor };
+    projects.push(projectData);
         const option = document.createElement('option');
-        option.value = newProject;
-        option.textContent = newProject;
+        option.value = newProject;
+        option.textContent = newProject;
+        option.style.color = projectColor;
         projectSelect.appendChild(option);
         projectSelect.value = newProject;
         currentProject = newProject;
@@ -367,9 +375,14 @@ function loadFromLocalStorage() {
     const storedProjects = localStorage.getItem('projects');
     if (storedProjects) {
         try {
             const loadedProjects = JSON.parse(storedProjects);
             // Add only new projects
             loadedProjects.forEach(project => {
-                if (!projects.includes(project)) {
-                    projects.push(project);
+                const projectName = typeof project === 'string' ? project : project.name;
+                const projectColor = typeof project === 'string' ? null : project.color;
+                const existingProject = projects.find(p => (typeof p === 'string' ? p : p.name) === projectName);
+                if (!existingProject) {
+                    const projectData = projectColor ? { name: projectName, color: projectColor } : projectName;
+                    projects.push(projectData);
                     const option = document.createElement('option');
-                    option.value = project;
-                    option.textContent = project;
+                    option.value = projectName;
+                    option.textContent = projectName;
+                    if (projectColor) {
+                        option.style.color = projectColor;
+                    }
                     projectSelect.appendChild(option);
                 }
             });
         } catch (e) {
             console.error('Error loading projects:', e);
         }
     }
 }
diff --git a/styles.css b/styles.css
new file mode 100644
--- /dev/null
+++ b/styles.css
@@ -0,0 +1,5 @@
+/* Dynamic project color styles */
+select option {
+    color: inherit; /* Ensures the option color is inherited from the JS color */
+}
+
+/* Additional styles can be placed here */
